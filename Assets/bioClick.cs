using UnityEngine;
using System.Collections;

public class bioClick : MonoBehaviour {

	string obj;

	void Update () {
		if (Input.GetMouseButtonUp (0)){
			gameObject.Find(gameObject.tag).GetComponent(
		}

	}
}
